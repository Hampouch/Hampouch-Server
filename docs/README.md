# 안내 — 이 폴더는 PR 리뷰 대상이 아닙니다

이 폴더는 서버 파트(정일혁)의 작업 문서 보관소예요. API 명세의 원본을 백업 목적으로 추적할 뿐이고, **팀이 보는 명세는 노션 "API 설계" 페이지**입니다 — 확정된 내용은 항상 거기에 올라갑니다.

루트의 md는 구현까지 끝난 확정본, `_drafts/`는 아직 확정 전인 초안(구현하면서 확정되면 루트로 올라옵니다)이에요. 어느 쪽이든 열어 보실 필요 없고, PR 리뷰는 `src/`만 봐 주시면 됩니다.
