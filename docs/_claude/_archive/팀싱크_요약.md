# 팀 싱크 요약 — 본 챌린지 서버 (정일혁)

> 1장 요약. 설계(ERD·API 명세)는 끝. **아래 공유 계약만 확정되면 구현 착수.**
> (구현은 Claude Code가 담당)
> ⚠️ 2026-07-05 역할 개편으로 일혁 범위 확장(홈 알림·위젯·미니챌린지 완수·AI 진단?) — 이 문서는 본 챌린지 기준, 신규는 `역할변경_영향분석.md`.
> ※ 본문의 "PM 질문 N번"은 재정렬 전 구 번호 — 현행은 `PM_질문목록.md`.

## 무엇을 만드나
본 챌린지 = 온보딩(기간·예산·약한 카테고리 설정) → **매일 지출로 성공/초과 판정** → 종료 시 성공/실패 결과 화면.
서버 산출물: ERD · API 명세(5개) · 설계초안.

## ERD 핵심
- **서버 소유**: `Challenge` · `ChallengeDay`(일별 판정) · `ChallengeWeakCategory`
- **외부 의존**: `User`(로그인=나연) · `Expense`(지출 데이터=령준, 2026-07-05 분담표 확정)

## 회의에서 확정할 것 (= 잠정표)

| # | 정할 것 | 누구 | 메모 |
|---|---|---|---|
| 1 | **DB 한 개 공유? + 네이밍/PK 컨벤션** | 팀(개발) | 마이그레이션·FK의 전제 |
| 2 | **유저 식별 — JWT `sub` / `user_id` 형식** | 나연(로그인) | `Challenge.user_id`가 여기 물림 |
| 3 | **지출 데이터 연동 방식**(공유 DB vs API vs 호출) | 령준+일혁 | 출처=령준 확정(2026-07-05 분담표) |
| 4 | 약한 카테고리 저장 단위(챌린지 vs 유저) | PM | PM 질문 2번 |
| 5 | 동시 진행 챌린지 1개? | 팀 | 중복 생성 시 409 가정 |

## 지금 안 막힌다
위 확정 전에도 **핵심 로직(판정·결과 계산)은 스텁+시드 데이터로 진행 가능.**
확정되면 스텁(userId·지출 입구·DB)만 진짜로 교체. → 일정 안 밀림.

---
*상세: `ERD.mermaid` · `API명세_본챌린지.md` · `구현착수_점검.md` · `PM_질문목록.md`*
