# 7/7(오늘) 11시 전체 회의 — 일혁 준비물 (이 한 장이면 됨)

## 0. 내 진행 상황 공유용 (한 줄씩)

- 설계: **ERD 확정판**(서버 소유 7테이블 + 외부 2) · **API 명세 완성**(본 챌린지 5개 + 확장 10개 엔드포인트) — 팀 ERDCloud 반영 완료
- 구현: 본 챌린지 Phase 1(엔티티·판정·결과 API) 스텁 위에서 진행 중
- 지난 회의(0630) 결정·와이어프레임 최신본 전부 설계에 반영 완료 (미입력=0원, 미니 재설계, 휴식, 한도 조정, 추천 화면 등)
- ※ 서버 회의 예정 안건이 "API 설계" → **`API명세_본챌린지.md` + `API명세_역할확장.md` 열어서 그대로 리뷰하면 됨**

## 1. PM에 물어볼 것 → **`PM_질문목록.md` 그대로 열어서** (현재 5개)

- 덤: `결정기록.md`의 "가정으로 정한 것들"(휴식·조정·미니·라운딩·기간 프리셋)은 PM에 훑어주고 "다르면 알려주세요"만.

## 2. 서버팀끼리 정할 것 (회의에서)

> 지출 연동·카테고리·위젯·추천 방식 등 기술 판단은 Claude가 이미 정해둠(`결정기록.md`). 회의에선 아래 3개만.

0. **[먼저 확인] 우리 앱 구조** — 하나의 스프링 부트 앱에 셋이 패키지로 나눠 들어가고 DB도 하나 공유하죠? (나연) → 맞으면 결정기록의 기술 결정 다 성립
1. **알림 — 누가 맡고 이번 스코프에 넣나?** '보류'였는데 화면이 제작됨. 챌린지·햄배틀·커뮤니티 전체 + 푸시 인프라(FCM) 필요. 역할·범위 결정이라 팀+PM
2. **팀 ERDCloud 정리** — Untitled 이름들, challenge의 `user_id2`(→user_id)만 정리. 가벼운 합의

> 령준에게 한마디 요청만 하면 되는 것(합의 아님): 지출 합계 조회 메서드 3종 제공 / 매칭 전 "휴식중?" 메서드 호출. 나에게 필요한 인터페이스라 요청하면 끝.

## 3. 디자이너에게 전달 (오타/싱크 2건)

- **'외식' 칩 중복** — 온보딩3 화면과 지출 입력-2 화면 둘 다 외식이 두 번 있음
- **스크린디자인 구버전 잔재** — 미니 커스텀 화면의 난이도·이모지(0630 삭제 결정분), 기간 버튼 30↔31 불일치 → 와이어프레임과 싱크

## 4. (선택) 회의 전에 피그마 열어보면 지울 수 있는 질문

- 질문 1(홈 스트립) → 홈에서 전날/다음날 선택한 상태 프레임 있나
- 질문 3(미니 버튼) → "새 챌린지 시작하기" 버튼의 프로토타입 연결 어디로
- 질문 5-①(미니 늦은 체크) → 미니 화면에서 어제로 넘긴 상태(완료 동그라미가 눌리나 회색이나)
