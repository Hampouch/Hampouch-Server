# 팀 노션 "API 설계" — 엔드포인트 상세 (붙여넣기용 · v4 — 코드블록판)

> 📌 **이 파일은 PR 리뷰 대상이 아닙니다** — 일혁이 확정 명세를 팀 노션에 옮겨 붙이는 작업용 원고입니다. 폴더 성격은 `docs/README.md` 참고.
>
> 🔍 **리뷰 동선 (0715 합의)** — 팀에 공개되는 건 반드시 일혁 눈을 거친다:
> 1) 확정분(돈 공식·에러 코드·응답 형태) 리뷰는 **이 파일의 갱신 블록에서** 한다 — 붙여넣기 직전이 관문.
> 2) 명세 md는 정기적으로 읽지 않는다 — 잠정/가정(명세 본문 ⚠️ 표시)은 회의 전 Claude가 안건 브리핑으로 정리해 준다.
>    확정분 본문 재독도 불필요 — 원본↔사본이 같다는 건 Claude가 갱신 때마다 숫자·필드명·에러코드·경로를 전수 대조해 "차이 0건"으로 보고한다.
>
> 쓰는 법 (딱 3단계):
> 1) 아래에서 한 엔드포인트의 **`▼▼▼ 복사 시작` 줄 다음부터 `▲▲▲ 복사 끝` 줄 전까지** 드래그해서 복사.
>    (▼▼▼ / ▲▲▲ 줄 자체는 복사하지 마세요. 그 사이 내용만.)
> 2) 팀 "API 설계" 페이지에서 그 엔드포인트 행에 마우스 올려 **"열기"** 클릭 → 본문 빈 곳 클릭.
> 3) **Cmd+Shift+V** (⚠️ 그냥 Cmd+V 아님!). Notion이 ## → 제목, ```json → 코드블록으로 변환 (나연 페이지와 동일).
>
> **블록별 반영 상태 (붙일 때마다 여기 갱신 — 상태가 의심되면 이 표 말고 노션 실사가 우선):**
> 복붙 허용 시점 = **PR 생성부터**(0717 일혁 결정 — 머지까지 안 기다림. 리뷰로 내용이 바뀌면 그 블록만 재붙여넣기).
> - ✅ 노션 반영 완료: **challenge 1~7 · challenge 10 · mini-challenge 1~5** (challenge 7 = 2026-07-27 부분 재붙여넣기 — 만료 후 포기가 실제 구현과 반대로 적혀 있어 에러 첫 줄·참고 3번째 줄 두 문단만 교체. 붙인 뒤 새로고침해 원고와 차이 0건 확인 / challenge 10 = 2026-07-27 밤 붙여넣기 완료, 페이지 본문을 다시 뽑아 원고와 차이 0건 확인. 행 상태도 "시작 전" → "개발완료")
> - 🟢 지금 붙이기(PR 열림): 없음
> - ⏳ PR 생성되면 붙이기: 없음
> - 🚫 미구현이라 붙이지 않음: challenge 8·9 · rest 1~2 (코드 없는 초안 — 노션엔 확정본만)


▼▼▼▼▼▼▼▼▼▼  복사 시작  ·  붙여넣을 곳 = [challenge 1 — 챌린지 생성 · POST /api/challenges] 행  ▼▼▼▼▼▼▼▼▼▼

## Request
```json
{
  "durationDays": 30,
  "budgetTotal": 100000,
  "startDate": "2026-06-23",
  "resetByPayday": true,
  "paydayDay": 25,
  "weakCategories": ["카페", "배달", "편의점"]
}
```

## Response (201 Created · Location: /api/challenges/{id})
```json
{
  "code": "SUCCESS",
  "message": "요청이 성공했습니다.",
  "data": {
    "challengeId": 1,
    "dailyLimit": 3333,
    "startDate": "2026-06-23",
    "endDate": "2026-07-22",
    "status": "IN_PROGRESS"
  }
}
```

## Error Response
- 필드 누락·형식·범위 위반(durationDays 1~100 — 상한 100일은 0714 전체회의 확정), resetByPayday=true인데 paydayDay 없음 (400)
```json
{ "code": "VALIDATION_ERROR", "message": "입력값이 올바르지 않습니다.", "status": 400,
  "fieldErrors": { "durationDays": "100 이하여야 합니다." } }
```
- 이미 진행 중(IN_PROGRESS) 챌린지 존재 — 동시 1개 가정 (409)
```json
{ "code": "CHALLENGE_ALREADY_IN_PROGRESS", "message": "이미 진행 중인 챌린지가 있습니다.", "status": 409 }
```
- 토큰 없음/유효하지 않음 (401)

▲▲▲▲▲▲▲▲▲▲  복사 끝  ▲▲▲▲▲▲▲▲▲▲


▼▼▼▼▼▼▼▼▼▼  복사 시작  ·  붙여넣을 곳 = [challenge 2 — 진행 중 챌린지 + 현황 · GET /api/challenges/current] 행  ▼▼▼▼▼▼▼▼▼▼

## Request
요청 바디 없음 (인증 헤더만). 진행 중이면 challenge/progress/consumption/warningCards/adjustment 5블록을 항상 함께 반환.

## Response (200 OK · 진행 중)
```json
{
  "code": "SUCCESS",
  "message": "요청이 성공했습니다.",
  "data": {
    "challenge": {
      "id": 1, "durationDays": 30, "startDate": "2026-06-23", "endDate": "2026-07-22",
      "budgetTotal": 100000, "dailyLimit": 3333, "status": "IN_PROGRESS"
    },
    "progress": {
      "elapsedDays": 5, "remainingDays": 25,
      "successDays": 4, "overDays": 1, "currentStreak": 2, "savedAmountSoFar": 4200
    },
    "consumption": {
      "todaySpent": 2000, "todayRemaining": 1333, "dailyLimit": 3333,
      "usageRate": 0.6, "character": "NORMAL", "alertLevel": "CAUTION"
    },
    "warningCards": [],
    "adjustment": { "usedCount": 0, "maxCount": 2 }
  }
}
```

## Response (200 OK · 휴식 중)
```json
{
  "code": "SUCCESS",
  "message": "요청이 성공했습니다.",
  "data": {
    "challenge": null,
    "rest": { "restStartDate": "2026-07-06", "plannedResumeDate": "2026-07-13" },
    "keptRecords": { "savedAmount": 68200, "maxStreak": 14 }
  }
}
```

## Error Response
- 진행 중 챌린지 없음(휴식도 없음) (404)
```json
{ "code": "NO_ACTIVE_CHALLENGE", "message": "진행 중인 챌린지가 없습니다.", "status": 404 }
```
- 인증 실패 (401)

> 참고: character = FULL/NORMAL/SKINNY, alertLevel = NONE/CAUTION/DANGER — 경계 둘 다 30/70(0714 통일, 필드는 분리 유지). progress 집계 구간은 기본 "시작일~어제"이고, 그 사이 기록 없는 날은 0원 지출(성공)로 간주해 센다(결과 화면과 같은 규칙 — 0714). 오늘은 아직 하루가 안 끝났으므로 기본 제외하되, 오늘 지출을 이미 입력했다면 오늘까지 포함해 계산. warningCards는 현재 GOAL_TOO_TIGHT(3일 연속 한도 초과)만 구현 — 오늘 사용률과 무관하게 발동, 미기록일이 지나가면 연속이 끊겨 사라짐. adjustment.usedCount는 조정 구현 전 0 고정. 위젯도 이 API 재사용 예정.

▲▲▲▲▲▲▲▲▲▲  복사 끝  ▲▲▲▲▲▲▲▲▲▲


▼▼▼▼▼▼▼▼▼▼  복사 시작  ·  붙여넣을 곳 = [challenge 3 — 캘린더(일별 기록) · GET /api/challenges/{id}/calendar?year=&month=] 행  ▼▼▼▼▼▼▼▼▼▼

## Request
Path: id (챌린지 id) · Query: year(1~9999, 필수 — 범위 밖 400 · 예 2026), month(1~12, 필수). 요청 바디 없음.

## Response (200 OK · 기록 있는 날만, 챌린지 기간 내)
```json
{
  "code": "SUCCESS",
  "message": "요청이 성공했습니다.",
  "data": {
    "challengeId": 1, "year": 2026, "month": 6,
    "days": [
      { "date": "2026-06-23", "status": "SUCCESS", "spentAmount": 2000 },
      { "date": "2026-06-24", "status": "OVER", "spentAmount": 5000 }
    ]
  }
}
```

## Error Response
- year/month 범위 밖 (400)
```json
{ "code": "BAD_REQUEST", "message": "잘못된 요청입니다.", "status": 400 }
```
- 남의 챌린지 (403)
```json
{ "code": "CHALLENGE_FORBIDDEN", "message": "해당 챌린지에 접근 권한이 없습니다.", "status": 403 }
```
- 챌린지 id 없음 (404)
```json
{ "code": "CHALLENGE_NOT_FOUND", "message": "챌린지를 찾을 수 없습니다.", "status": 404 }
```
- 인증 실패 (401)

▲▲▲▲▲▲▲▲▲▲  복사 끝  ▲▲▲▲▲▲▲▲▲▲


▼▼▼▼▼▼▼▼▼▼  복사 시작  ·  붙여넣을 곳 = [challenge 4 — 종료 결과 · GET /api/challenges/{id}/result] 행  ▼▼▼▼▼▼▼▼▼▼

## Request
Path: id (종료된 SUCCESS/FAIL 챌린지). 요청 바디 없음.

## Response (200 OK)
```json
{
  "code": "SUCCESS",
  "message": "요청이 성공했습니다.",
  "data": {
    "challengeId": 1, "status": "SUCCESS",
    "period": { "startDate": "2026-05-01", "endDate": "2026-05-14", "durationDays": 14 },
    "summary": {
      "successDays": 14, "overDays": 0, "savedAmount": 68200, "overAmount": 0,
      "maxStreak": 14, "budgetTotal": 280000, "actualSpent": 211800
    },
    "categoryBreakdown": [ { "category": "배달", "amount": 38400 }, { "category": "카페", "amount": 23500 } ],
    "emotionBreakdown": [ { "emotion": "먹고싶어서", "ratio": 0.42 }, { "emotion": "스트레스", "ratio": 0.31 } ]
  }
}
```

## Error Response
- 아직 IN_PROGRESS(결과 미확정) → current 사용 (409)
```json
{ "code": "CHALLENGE_NOT_ENDED", "message": "아직 진행 중인 챌린지입니다.", "status": 409 }
```
- 챌린지 id 없음 (404) · 남의 챌린지 (403) · 인증 실패 (401)

> 참고: categoryBreakdown/emotionBreakdown은 령준 지출(EXPENSE) 연동 전 빈 배열([]). 미입력일=0원=SUCCESS 간주(0630) — 기록 0건이어도 전원 성공=SUCCESS 확정(0714, RESULT_NO_RECORD 409 제거). status는 end_date 경과 시 확정 저장(전부 SUCCESS→SUCCESS, OVER 1+→FAIL), 이후 기간 내 지출 수정 시 다시 계산됨(0714).

▲▲▲▲▲▲▲▲▲▲  복사 끝  ▲▲▲▲▲▲▲▲▲▲


▼▼▼▼▼▼▼▼▼▼  복사 시작  ·  붙여넣을 곳 = [challenge 5 — 일별 지출 수신·판정 · POST /api/challenges/{id}/days] 행  ▼▼▼▼▼▼▼▼▼▼

## Request
```json
{
  "date": "2026-06-24",
  "spentAmount": 5000
}
```

## Response (200 OK · upsert)
```json
{
  "code": "SUCCESS",
  "message": "요청이 성공했습니다.",
  "data": {
    "date": "2026-06-24",
    "spentAmount": 5000,
    "dailyLimit": 3333,
    "status": "OVER"
  }
}
```

## Error Response
- date가 챌린지 기간 밖 (400)
```json
{ "code": "DAY_OUT_OF_RANGE", "message": "날짜가 챌린지 기간 밖입니다.", "status": 400 }
```
- spentAmount < 0 등 검증 실패 (400)
```json
{ "code": "VALIDATION_ERROR", "message": "입력값이 올바르지 않습니다.", "status": 400, "fieldErrors": { "spentAmount": "0 이상이어야 합니다." } }
```
- 남의 챌린지 (403) · 챌린지 id 없음 (404) · 인증 실패 (401)

> 참고: 판정 status = spentAmount ≤ dailyLimit ? SUCCESS : OVER. 운영에선 령준 지출 조회 메서드로 대체 예정 — 이 엔드포인트는 테스트/시드 전용.

▲▲▲▲▲▲▲▲▲▲  복사 끝  ▲▲▲▲▲▲▲▲▲▲


▼▼▼▼▼▼▼▼▼▼  복사 시작  ·  붙여넣을 곳 = [challenge 6 — 지난 챌린지 리스트 · GET /api/challenges/history] 행  ▼▼▼▼▼▼▼▼▼▼

## Request
요청 바디 없음. 종료(SUCCESS/FAIL) 챌린지만, endDate 내림차순(최근 먼저).

## Response (200 OK · 없으면 items: [])
```json
{
  "code": "SUCCESS",
  "message": "요청이 성공했습니다.",
  "data": {
    "items": [
      { "challengeId": 12, "status": "SUCCESS",
        "startDate": "2026-05-01", "endDate": "2026-05-14", "durationDays": 14,
        "budgetTotal": 280000, "actualSpent": 211800, "savedAmount": 68200 },
      { "challengeId": 8, "status": "FAIL",
        "startDate": "2026-04-01", "endDate": "2026-04-14", "durationDays": 14,
        "budgetTotal": 210000, "actualSpent": 224000, "savedAmount": 41000 }
    ]
  }
}
```

## Error Response
- 인증 실패 (401)

> 참고: 각 챌린지 상세는 GET /{id}/result 재사용. actualSpent/savedAmount는 령준 연동 전 시드.
> 기간이 끝난 뒤 유저가 결과 화면을 아직 안 열었으면 서버엔 SUCCESS/FAIL이 저장되기 전인데(정해진 시각에 자동 확정하는 배치 작업은 없고 조회 때 확정), 그런 챌린지도 이 리스트 조회가 그 자리에서 확정해 포함한다 — 결과 화면을 거치지 않아도 히스토리에서 빠지지 않는다.
> endDate가 같으면 나중에 만든 챌린지가 먼저 온다(보조 정렬).

▲▲▲▲▲▲▲▲▲▲  복사 끝  ▲▲▲▲▲▲▲▲▲▲


▼▼▼▼▼▼▼▼▼▼  복사 시작  ·  붙여넣을 곳 = [challenge 7 — 중도 포기 · POST /api/challenges/{id}/give-up] 행  ▼▼▼▼▼▼▼▼▼▼

## Request
요청 바디 없음. 진행 중 챌린지를 즉시 FAIL로 종료(0707: 도중 중단=실패).

## Response (200 OK)
```json
{
  "code": "SUCCESS",
  "message": "요청이 성공했습니다.",
  "data": { "challengeId": 1, "status": "FAIL" }
}
```

## Error Response
- 진행 중이 아닌 챌린지(이미 종료됨, 또는 기간이 지나 곧 확정될 챌린지) (409)
```json
{ "code": "CHALLENGE_NOT_IN_PROGRESS", "message": "이미 종료된 챌린지입니다.", "status": 409 }
```
- 남의 챌린지 (403) · 챌린지 id 없음 (404) · 인증 실패 (401)

> 참고: FAIL 확정 후 GET /{id}/result 조회 가능. end_date는 원래 목표 기간 보존.
> 포기의 FAIL은 유저 선언이라, 이후 기간 내 지출을 수정해도 재계산(0714 규칙)으로 SUCCESS로 되살아나지 않음(재계산 제외). 기록 수정 자체는 허용.
> end_date가 지났는데 미확정(결과 화면 미조회)인 챌린지에 포기가 오면, 상태를 바꾸지 않고 409로 거절 — 완주 결과가 뒤늦은 그만두기로 FAIL이 되지 않음. 이 챌린지의 확정(SUCCESS/FAIL)은 결과·히스토리 조회가 맡는다. end_date 당일까지는 포기 가능.

▲▲▲▲▲▲▲▲▲▲  복사 끝  ▲▲▲▲▲▲▲▲▲▲


▼▼▼▼▼▼▼▼▼▼  복사 시작  ·  붙여넣을 곳 = [challenge 8 — 한도 조정 · POST /api/challenges/{id}/adjust] 행  ▼▼▼▼▼▼▼▼▼▼

## Request
```json
{
  "option": "PLUS_10"
}
```
option = PLUS_10 / PLUS_20 (서버가 현재 한도 ×1.1 / ×1.2, 버림). "현재 유지"는 호출 없음. 챌린지당 최대 2회. 하향 없음.

## Response (200 OK)
```json
{
  "code": "SUCCESS",
  "message": "요청이 성공했습니다.",
  "data": { "challengeId": 1, "dailyLimit": 27500, "usedCount": 1, "maxCount": 2 }
}
```

## Error Response
- option이 PLUS_10/PLUS_20 아님 (400)
```json
{ "code": "VALIDATION_ERROR", "message": "입력값이 올바르지 않습니다.", "status": 400 }
```
- 조정 횟수 소진(2/2) · 챌린지 IN_PROGRESS 아님 (409)
```json
{ "code": "ADJUSTMENT_LIMIT_EXCEEDED", "message": "한도 조정 횟수를 모두 사용했습니다.", "status": 409 }
```
- 남의 챌린지 (403) · 챌린지 id 없음 (404) · 인증 실패 (401)

> 참고: 과거 challenge_day 판정은 소급 안 함(각 행 daily_limit 스냅샷 유지). 트리거 = 최근 3일 연속 한도 초과.

▲▲▲▲▲▲▲▲▲▲  복사 끝  ▲▲▲▲▲▲▲▲▲▲


▼▼▼▼▼▼▼▼▼▼  복사 시작  ·  붙여넣을 곳 = [challenge 9 — 다음 챌린지 추천 · GET /api/challenges/recommendation] 행  ▼▼▼▼▼▼▼▼▼▼

## Request
요청 바디 없음. 직전 종료 챌린지 기반 프리필(성공 후/실패 후 새 챌린지 설계 화면).

## Response (200 OK)
```json
{
  "code": "SUCCESS",
  "message": "요청이 성공했습니다.",
  "data": {
    "lastChallenge": {
      "durationDays": 30, "status": "SUCCESS", "budgetTotal": 400000, "actualSpent": 340000,
      "tags": ["60,000원 아낌", "목표 대비 15%↓", "28일 기록 유지"]
    },
    "recommendation": {
      "durationDays": 30, "budgetTotal": 330000, "dailyLimit": 11000,
      "focusCategories": ["배달", "카페"],
      "message": "목표보다 60,000원이나 아꼈어요. 이 흐름을 이어가되 이번엔 살짝 더 타이트하게 가봐요."
    }
  }
}
```

## Error Response
- 종료된 챌린지 없음(첫 챌린지는 온보딩 직접 설정) (404)
```json
{ "code": "CHALLENGE_NOT_FOUND", "message": "챌린지를 찾을 수 없습니다.", "status": 404 }
```
- 인증 실패 (401)

> 참고: 문구(message)는 룰 기반 생성(0711 PM: AI 안 씀). 수치 % 공식은 기획 확인 대기 — 확정 전 시드/룰 스텁. 실패 태그는 령준 지출 감정·요일 집계 의존.

▲▲▲▲▲▲▲▲▲▲  복사 끝  ▲▲▲▲▲▲▲▲▲▲


▼▼▼▼▼▼▼▼▼▼  복사 시작  ·  붙여넣을 곳 = [challenge 10 — 집중 카테고리 수정 · PUT /api/challenges/{id}/focus-categories] 행  ▼▼▼▼▼▼▼▼▼▼

## Request
```json
{
  "categories": ["배달", "카페", "배달"]
}
```
전체 교체(같은 요청을 여러 번 보내도 결과 동일). 보낸 목록이 곧 최종 상태 — 빠진 카테고리는 해제된다.
값은 기본 7종(배달/외식/편의점/카페/간식/장보기/술자리) + 자유 문자열 — 서버는 값 목록을 제한하지 않는다. categories는 필수 필드이고, 빈 배열 `[]`은 "전부 해제"로 허용.
같은 카테고리를 여러 번 보내면 서버가 한 개로 접어 저장한다.

## Response (200 OK)
```json
{
  "code": "SUCCESS",
  "message": "요청이 성공했습니다.",
  "data": { "challengeId": 1, "categories": ["배달", "카페"] }
}
```
교체가 끝난 뒤의 최종 목록(중복 제거 반영). 순서는 요청에 보낸 순서.

## Error Response
- categories 누락, 원소가 빈 문자열·공백, 원소가 50자 초과 (400)
```json
{ "code": "VALIDATION_ERROR", "message": "입력값이 올바르지 않습니다.", "status": 400,
  "fieldErrors": { "categories[1]": "must not be blank" } }
```
fieldErrors 키는 목록 자체가 잘못이면 `categories`, 원소가 잘못이면 `categories[인덱스]`(0부터) — 목록 몇 번째 항목이 걸렸는지는 이 인덱스로 안다.
fieldErrors의 값(문구)은 서버가 정한 문장이 아니라 검증 라이브러리가 주는 기본 문구다 — 요청 헤더 Accept-Language에 따라 언어가 갈리고(ko를 보내면 한글, 안 보내면 영어) 라이브러리 버전이 올라가면 문장이 바뀔 수 있다. 고정된 건 code·status·fieldErrors의 키까지다.
반면 위의 message "입력값이 올바르지 않습니다."는 서버 코드에 박아둔 값이라 헤더와 무관하게 항상 같다.
- 진행 중이 아닌 챌린지(종료됨, 또는 기간이 지나 곧 확정될 챌린지) (409)
```json
{ "code": "CHALLENGE_NOT_IN_PROGRESS", "message": "이미 종료된 챌린지입니다.", "status": 409 }
```
- 남의 챌린지 (403) · 챌린지 id 없음 (404) · 인증 실패 (401)

▲▲▲▲▲▲▲▲▲▲  복사 끝  ▲▲▲▲▲▲▲▲▲▲


▼▼▼▼▼▼▼▼▼▼  복사 시작  ·  붙여넣을 곳 = [rest 1 — 휴식 시작 · POST /api/rests] 행  ▼▼▼▼▼▼▼▼▼▼

## Request
```json
{
  "restDays": 7
}
```
restDays ≥ 1 (화면의 3일/1주/2주/직접선택을 일수로 통일). 유저 단위.

## Response (201 Created)
```json
{
  "code": "SUCCESS",
  "message": "요청이 성공했습니다.",
  "data": { "restId": 3, "restStartDate": "2026-07-06", "plannedResumeDate": "2026-07-13" }
}
```

## Error Response
- restDays < 1 (400)
```json
{ "code": "VALIDATION_ERROR", "message": "입력값이 올바르지 않습니다.", "status": 400 }
```
- 이미 휴식 중 · 진행 중(IN_PROGRESS) 챌린지 존재 (409)
```json
{ "code": "REST_ALREADY_ACTIVE", "message": "이미 휴식 중입니다.", "status": 409 }
```
- 인증 실패 (401)

> 참고: 진입점 = 결과 화면뿐(진행 중 휴식 진입 불가). 휴식 중 POST /challenges 오면 휴식 자동 종료 후 생성.

▲▲▲▲▲▲▲▲▲▲  복사 끝  ▲▲▲▲▲▲▲▲▲▲


▼▼▼▼▼▼▼▼▼▼  복사 시작  ·  붙여넣을 곳 = [rest 2 — 복귀 / 더 쉬기 · POST /api/rests/resume] 행  ▼▼▼▼▼▼▼▼▼▼

## Request
```json
{
  "when": "NOW"
}
```
when = NOW(지금 바로) / TOMORROW(내일부터) / EXTEND(조금 더 쉴게요). EXTEND면 extendDays(≥1) 필수.
```json
{ "when": "EXTEND", "extendDays": 7 }
```

## Response (200 OK)
```json
{
  "code": "SUCCESS",
  "message": "요청이 성공했습니다.",
  "data": { "restId": 3, "resumeDate": "2026-07-07" }
}
```
EXTEND면 data = { "restId": 3, "plannedResumeDate": "2026-07-20" }

## Error Response
- when 오류 · EXTEND인데 extendDays 없음 (400)
```json
{ "code": "VALIDATION_ERROR", "message": "입력값이 올바르지 않습니다.", "status": 400 }
```
- 휴식 중 아님 (404)
```json
{ "code": "REST_NOT_ACTIVE", "message": "휴식 중이 아닙니다.", "status": 404 }
```
- 인증 실패 (401)

▲▲▲▲▲▲▲▲▲▲  복사 끝  ▲▲▲▲▲▲▲▲▲▲


▼▼▼▼▼▼▼▼▼▼  복사 시작  ·  붙여넣을 곳 = [mini-challenge 1 — 그날 미니 조회 · GET /api/mini-challenges?date=] 행  ▼▼▼▼▼▼▼▼▼▼

## Request
Query: date (생략 시 오늘). 유저 소유 조회(본챌 독립).

## Response (200 OK)
```json
{
  "code": "SUCCESS",
  "message": "요청이 성공했습니다.",
  "data": {
    "date": "2026-07-06",
    "summary": { "checkedCount": 3, "totalCount": 4, "streakDays": 3 },
    "items": [
      { "miniChallengeId": 5, "title": "오늘 커피 사먹지 않기",
        "durationDays": 7, "progressDays": 4, "itemStreak": 3, "checked": true }
    ]
  }
}
```

## Error Response
- date 형식 오류 (400 `BAD_REQUEST`) · 미래 date (400 `MINI_FUTURE_DATE` — 날짜 스트립이 미래 이동 불가라 조회 상황 없음) · 인증 실패 (401)
```json
{ "code": "MINI_FUTURE_DATE", "message": "미래 날짜는 조회할 수 없습니다.", "status": 400 }
```

> 참고: 집계(checkedCount/totalCount/streakDays/progressDays/itemStreak)는 저장값이 아니라 조회 때마다 계산하며, 기준점은 조회 date(as-of) — date 이후의 체크는 반영되지 않고 경과일·활성 여부·스트릭 모두 date 기준. streakDays = "그날 진행 중이던 미니를 전부 체크한 날"이 date부터 거꾸로 몇 일 연속인지(전부의 기준은 날마다 그날 활성 미니).

▲▲▲▲▲▲▲▲▲▲  복사 끝  ▲▲▲▲▲▲▲▲▲▲


▼▼▼▼▼▼▼▼▼▼  복사 시작  ·  붙여넣을 곳 = [mini-challenge 2 — 추천 목록 · GET /api/mini-challenges/recommended?durationDays=] 행  ▼▼▼▼▼▼▼▼▼▼

## Request
Query: durationDays (기간 탭 필터 — 오늘만/3/7/14/31, 생략 시 전체). 공용 추천 카탈로그(읽기 전용), 노출 랜덤.

## Response (200 OK)
```json
{
  "code": "SUCCESS",
  "message": "요청이 성공했습니다.",
  "data": {
    "items": [
      { "recommendedId": 7, "title": "편의점 디저트 안 먹기", "durationDays": 7 }
    ]
  }
}
```

## Error Response
- 인증 실패 (401)

> 참고: recommendedId는 공용 추천 목록의 id이고, 유저의 미니 챌린지 id가 아니다. 이 값을 미니 추가(mini-challenge 3)에 보내면 서버가 추천 목록에서 그 항목의 제목·기간을 복사해 유저 소유의 미니 챌린지를 새로 만들고, 응답으로 새 miniChallengeId를 준다. 이후 체크·삭제는 그 miniChallengeId로 한다.
> 참고: 추천을 추가해도 원본을 참조하는 게 아니라 값을 복사해 오므로, 기획이 나중에 추천 목록의 문구를 바꾸거나 항목을 빼도 이미 추가한 유저의 미니 챌린지는 그대로 유지된다.
> 참고: durationDays가 1·3·7·14·31 밖이면 에러가 아니라 items가 빈 배열인 200이다. 그 기간의 추천이 없다는 뜻.

▲▲▲▲▲▲▲▲▲▲  복사 끝  ▲▲▲▲▲▲▲▲▲▲


▼▼▼▼▼▼▼▼▼▼  복사 시작  ·  붙여넣을 곳 = [mini-challenge 3 — 미니 추가 · POST /api/mini-challenges] 행  ▼▼▼▼▼▼▼▼▼▼

## Request
둘 중 한 형태만 (둘 다/둘 다 아님 → 400).
```json
{ "recommendedId": 7 }
```
또는
```json
{ "custom": { "title": "오늘 간식 2개 이하", "durationDays": 7 } }
```

## Response (201 Created · Location: /api/mini-challenges/{id})
```json
{
  "code": "SUCCESS",
  "message": "요청이 성공했습니다.",
  "data": {
    "miniChallengeId": 5, "title": "오늘 간식 2개 이하",
    "durationDays": 7, "startDate": "2026-07-06", "endDate": "2026-07-12"
  }
}
```

## Error Response
- 바디 형태 위반(추천/커스텀 동시·부재) (400)
```json
{ "code": "MINI_INVALID_BODY", "message": "추천/커스텀 중 하나만 보내야 합니다.", "status": 400 }
```
- durationDays가 {1,3,7,14,31} 밖 (400)
```json
{ "code": "MINI_INVALID_DURATION", "message": "허용되지 않는 기간입니다.", "status": 400 }
```
- recommendedId 카탈로그에 없음 (404 `MINI_RECOMMENDED_NOT_FOUND`) · 인증 실패 (401)
```json
{ "code": "MINI_RECOMMENDED_NOT_FOUND", "message": "추천 미니 챌린지를 찾을 수 없습니다.", "status": 404 }
```

▲▲▲▲▲▲▲▲▲▲  복사 끝  ▲▲▲▲▲▲▲▲▲▲


▼▼▼▼▼▼▼▼▼▼  복사 시작  ·  붙여넣을 곳 = [mini-challenge 4 — 미니 삭제 · DELETE /api/mini-challenges/{miniChallengeId}] 행  ▼▼▼▼▼▼▼▼▼▼

## Request
Path: miniChallengeId. 요청 바디 없음. (수정 불가 정책 → 삭제 후 재생성)

## Response (204 No Content)
본문 없음.

## Error Response
- 남의 미니 (403) · 미니 없음 (404) · 인증 실패 (401)
```json
{ "code": "MINI_NOT_FOUND", "message": "미니 챌린지를 찾을 수 없습니다.", "status": 404 }
```

> 참고: 미니 행 + 그 일별 체크(mini_challenge_day) 함께 삭제.

▲▲▲▲▲▲▲▲▲▲  복사 끝  ▲▲▲▲▲▲▲▲▲▲


▼▼▼▼▼▼▼▼▼▼  복사 시작  ·  붙여넣을 곳 = [mini-challenge 5 — 일별 체크/해제 · PUT /api/mini-challenges/{miniChallengeId}/check] 행  ▼▼▼▼▼▼▼▼▼▼

## Request
```json
{
  "date": "2026-07-06",
  "checked": true
}
```
date 생략 시 오늘 · 과거 허용 · 미래는 400 차단. checked true=체크(upsert) / false=해제(행 삭제). PUT 멱등.

## Response (200 OK)
```json
{
  "code": "SUCCESS",
  "message": "요청이 성공했습니다.",
  "data": { "miniChallengeId": 5, "date": "2026-07-06", "checked": true }
}
```

## Error Response
- date 미니 기간 밖 (400 `MINI_DATE_OUT_OF_RANGE`) · 미래 날짜 (400 `MINI_FUTURE_CHECK` — 미래이면서 기간 밖이기도 하면 이쪽 우선)
```json
{ "code": "MINI_FUTURE_CHECK", "message": "미래 날짜는 체크할 수 없습니다.", "status": 400 }
```
- 남의 미니 (403) · 미니 없음 (404) · 인증 실패 (401)
```json
{ "code": "MINI_NOT_FOUND", "message": "미니 챌린지를 찾을 수 없습니다.", "status": 404 }
```

▲▲▲▲▲▲▲▲▲▲  복사 끝  ▲▲▲▲▲▲▲▲▲▲
